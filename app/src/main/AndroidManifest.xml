<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aymenace.blogdroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name="com.aymenace.blogdroid.CommentActivity"></activity>
        <activity android:name="com.aymenace.blogdroid.EditPostActivity" />
        <activity android:name="com.aymenace.blogdroid.AddPostActivity" />
        <activity android:name="com.aymenace.blogdroid.HomeActivity" />
        <activity android:name="com.aymenace.blogdroid.UserInfoActivity" />
        <activity android:name="com.aymenace.blogdroid.OnBoardActivity" />
        <activity android:name="com.aymenace.blogdroid.AuthActivity" />
        <activity android:name="com.aymenace.blogdroid.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>